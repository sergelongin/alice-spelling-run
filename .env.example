# Alice Spelling Run - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# SUPABASE CONFIGURATION (Required for auth and data sync)
# =============================================================================
# Get these from: https://supabase.com/dashboard/project/_/settings/api
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# =============================================================================
# DATABASE CREDENTIALS (For running migrations via Supabase CLI)
# =============================================================================
# These are NOT used by the app at runtime - only for CLI migration commands
# Get passwords from: Supabase Dashboard → Project Settings → Database → Connection string

# Development database password (used with: source .env.development && supabase db push -p "$DEV_DB_PASSWORD")
DEV_PROJECT_REF=kphvkkoyungqebftytkt
DEV_DB_PASSWORD=your-dev-database-password

# Production database password (used with: source .env && supabase db push -p "$VITE_SUPABASE_DBPASSWPRD")
# Note: Prod credentials should be in .env (not .env.development)
VITE_SUPABASE_DBPASSWPRD=your-prod-database-password

# =============================================================================
# AI PROVIDER CONFIGURATION
# =============================================================================
# Choose your AI provider: 'openai', 'anthropic', or 'groq'
VITE_AI_PROVIDER=openai

# API Keys - Only the active provider's key is required
# Get your key from: https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=

# Get your key from: https://console.anthropic.com/settings/keys
VITE_ANTHROPIC_API_KEY=

# Get your key from: https://console.groq.com/keys
VITE_GROQ_API_KEY=

# =============================================================================
# TEXT-TO-SPEECH CONFIGURATION (Cartesia)
# =============================================================================
# Optional - falls back to browser TTS if not configured
# Pre-generated audio from Supabase takes priority over Cartesia

# Get your key from: https://play.cartesia.ai
VITE_CARTESIA_API_KEY=

# Browse voices at: https://play.cartesia.ai/voices (optional, uses default if not set)
VITE_CARTESIA_VOICE_ID=

# Emotion: enthusiastic, happy, curious, content, calm, grateful, affectionate, excited, peaceful
VITE_CARTESIA_EMOTION=enthusiastic

# Speed: 0.6 to 1.5 (default 1.0, slightly slower may be clearer for kids)
VITE_CARTESIA_SPEED=0.95

# Audio generation concurrency limit for batch operations (1-20)
# Cartesia plan limits: Free=2, Pro=3, Startup=5, Scale=10-15
# Default is 3 (optimized for Pro plan)
VITE_AUDIO_GENERATION_CONCURRENCY=3

# =============================================================================
# SYNC DEBUGGING
# =============================================================================
# Enable detailed sync logging to diagnose data sync issues
# Shows: server response validation, reconciliation stats, post-sync verification
# Default is false (minimal logging)
VITE_SYNC_DEBUG=false

# =============================================================================
# LAUNCHDARKLY (Feature Flags & Observability)
# =============================================================================
# Optional - app runs normally without LaunchDarkly if not configured
# Get your client-side ID from: LaunchDarkly Dashboard → Project Settings → Environments
VITE_LAUNCHDARKLY_CLIENT_ID=
